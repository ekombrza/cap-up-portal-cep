import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cap-up-portal-cep',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
